

#include <iostream>
#include <cstdlib>
#include <ctime>
#include <cmath>

using namespace std;

int main() {
    srand(time(0));
    int a, b, c, d;

    // а) Ладья не угрожает полю (c, d)
    do {
        a = rand() % 8 + 1; b = rand() % 8 + 1;
        c = rand() % 8 + 1; d = rand() % 8 + 1;
    } while (a == c || b == d); // Угрожает, если на той же вертикали или горизонтали

    cout << "Задача а): Позиции (" << a << "," << b << ") и (" << c << "," << d << "). Ладья не угрожает." << endl;

    // б) Слон не угрожает полю (c, d)
    do {
        a = rand() % 8 + 1; b = rand() % 8 + 1;
        c = rand() % 8 + 1; d = rand() % 8 + 1;
    } while (abs(a - c) == abs(b - d)); // Угрожает, если на одной диагонали

    cout << "Задача б): Позиции (" << a << "," << b << ") и (" << c << "," << d << "). Слон не угрожает." << endl;

    // в) Король может одним ходом попасть на поле (c, d)
    do {
        a = rand() % 8 + 1; b = rand() % 8 + 1;
        c = rand() % 8 + 1; d = rand() % 8 + 1;
    } while (!(abs(a - c) <= 1 && abs(b - d) <= 1 && (a != c || b != d))); // Условие хода короля

    cout << "Задача в): Позиции (" << a << "," << b << ") и (" << c << "," << d << "). Король может пойти." << endl;

    // г) Ферзь не угрожает полю (c, d)
    do {
        a = rand() % 8 + 1; b = rand() % 8 + 1;
        c = rand() % 8 + 1; d = rand() % 8 + 1;
    } while (a == c ⠞⠺⠵⠟⠵⠟⠵⠺ abs(a - c) == abs(b - d)); // Угрожает, как ладья или слон

    cout << "Задача г): Позиции (" << a << "," << b << ") и (" << c << "," << d << "). Ферзь не угрожает." << endl;

    return 0;
}
