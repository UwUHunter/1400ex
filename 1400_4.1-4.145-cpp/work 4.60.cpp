#include
void solve460(double height1, double height2, double height3) {
if (height1 == height2 && height2 == height3) {
std::cout << "Рост всех трех человек одинаков." << std::endl;
} else {
std::cout << "Рост людей не одинаков." << std::endl;
}
}
