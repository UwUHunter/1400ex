#include
int main() {
int a1, a2, a3, b1, b2;
std::cout << "Введите цифры трехзначного числа (сотни, десятки, единицы): ";
std::cin >> a3 >> a2 >> a1;
std::cout << "Введите цифры двузначного числа (десятки, единицы): ";
std::cin >> b2 >> b1;

int A = a3 * 100 + a2 * 10 + a1;
int B = b2 * 10 + b1;
int C = A + B;

int c3 = C / 100;
int c2 = (C % 100) / 10;
int c1 = C % 10;

std::cout << "Цифры суммы: " << c3 << " (сотни), " << c2 << " (десятки), " << c1 << " (единицы)" << std::endl;

return 0;
}
